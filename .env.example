# Google Gemini API Key
# Dapatkan API key dari https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your-gemini-api-key-here

# Public site URL — used for canonical URLs, sitemap, OG tags.
# In production set this to your deployed origin (no trailing slash).
NEXT_PUBLIC_SITE_URL=https://kilascerdas.com
